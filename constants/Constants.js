let SERVICE_PROTOCOL = "http://";
let SERVICE_HOST = "thaiwellnesstourism.com";
let SERVICE_PORT = "/";
let SERVICE_CONTEXT = "api/web/"
let SERVICE_VERSION = "v1.0"

module.exports = {
    URL_SERVICE: SERVICE_PROTOCOL + SERVICE_HOST + SERVICE_PORT + SERVICE_CONTEXT + SERVICE_VERSION,
}